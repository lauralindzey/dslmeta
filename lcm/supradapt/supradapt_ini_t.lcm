package supradapt;

struct supradapt_ini_t
{
  string channel_update_directive;
  string channel_profile;
  string channel_statexy;

  int8_t scheme;

  int16_t num_depths;
  double depths[num_depths];
  
  double profile_stop_depth;

  supradapt_scheme_min_depth_interval_parameters_t scheme_center_max_parameters;	
  supradapt_scheme_min_depth_interval_parameters_t scheme_min_depth_interval_parameters;

  string host;

}
